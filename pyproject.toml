[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "algcom"
version = "0.1.0"
description = "Combinatorial Hopf algebra primitives with sparse vectors and tensors"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
